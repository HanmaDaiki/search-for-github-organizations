import { SearchForm } from '@features/searchForAnOrganization';

export const MainPage = () => {
  return (
    <main className='w-screen h-screen flex flex-col items-center justify-center'>
      <SearchForm />
    </main>
  );
};
